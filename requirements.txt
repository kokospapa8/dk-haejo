# Discord
discord.py[voice]>=2.4.0
PyNaCl==1.5.0

# YouTube audio
yt-dlp>=2025.3.1

# EJS (External JavaScript) for yt-dlp — provides signature/n-challenge solver
# scripts as a pip package so yt-dlp never needs to download them from GitHub
# at runtime. Avoids cache-volume shadowing the Dockerfile build-time cache.
# https://pypi.org/project/yt-dlp-ejs/
yt-dlp-ejs>=0.8.0

# PO Token yt-dlp plugin — auto-connects to bgutil sidecar at localhost:4416
# Injects Proof-of-Origin tokens so the `web` client works on cloud IPs
# https://github.com/Brainicism/bgutil-ytdlp-pot-provider
bgutil-ytdlp-pot-provider

# Anthropic LLM
anthropic>=0.40.0

# Config
python-dotenv==1.0.1
